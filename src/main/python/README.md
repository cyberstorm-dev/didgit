# git-attest Python

Utilities for fetching and analyzing attestations and on-chain data.

Examples:
- `src/git_attest/fetch_attestation.py` – fetch attestation by UID from EASScan GraphQL.

Install (with uv):
- `uv sync`
- `uv run python -m git_attest.fetch_attestation --uid <UID>`

